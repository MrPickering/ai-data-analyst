# Example Output

This output was generated from `data/questions/basic-queries.json` against `data/sales.db` using Claude Sonnet 4.

No API key is needed to browse these results — they're pre-generated so you can see exactly what the tool produces.

## Files

| File | Description |
|------|-------------|
| `query-results/` | SQL queries + results for each basic question |
| `data-quality-report.json` | Detected data quality issues with severity and fix suggestions |
| `narrative-report.md` | Executive narrative analysis for the top products query |
| `anomaly-report.json` | Statistical anomalies and outliers detected in the dataset |
| `benchmark.json` | Timing and token usage for the full benchmark run |

## Sample Questions

1. What were our top 5 products by revenue last quarter?
2. How many new customers did we get each month this year?
3. What's our average order value by customer segment?
4. Which sales rep has the highest quota attainment?
5. What are our most returned products?
